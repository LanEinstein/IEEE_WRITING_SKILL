# /ieee-write (IEEE Paper Suite)

Run the ieee-write workflow of the IEEE Paper Suite.

1. The suite root is `{{IEEE_SKILL_ROOT}}`.
2. Read `{{IEEE_SKILL_ROOT}}/skills/ieee-write/SKILL.md` IN FULL.
3. Follow the stages exactly. Wherever the SKILL says `<suite>`, use the
   root above. Read every named core file in full before the stage that
   needs the file.
4. Hard gates are mandatory: outline approval before drafting, and an
   explicit `go` per paragraph. Propose first, act only after approval.
